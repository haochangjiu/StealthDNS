# StealthDNS
**StealthDNS** is an open source client daemon that runs as a **local DNS server** on endpoints or edge nodes.   It intercepts DNS lookups, applies **Zero Trust policies**, and performs **NHP knocking** (Network-infrastructure Hiding Protocol) before revealing protected services.
